import { StyleSheet } from 'react-native'
import { backgroundColor, textColor } from 'styles/colors'
export const mainLoginStyles = StyleSheet.create({
  buttonsContainer: {
    // flex:1,
    //height:250,
    justifyContent: 'space-between',
    gap: 20,
    paddingVertical: 25,
  },
})
